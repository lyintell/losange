EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Build EAS (preview + production) : pousser ces variables vers Expo
# npx eas env:push --environment preview
# npx eas env:push --environment production

# Auth MASTER admin web : secrets Supabase uniquement (voir supabase/functions/README.md)
# MASTER_ADMIN_IDENTIFIANT
# MASTER_ADMIN_PASSWORD
# MASTER_SESSION_SECRET

# Auth terrain mobile : Edge Function terrain-login (deployer via supabase/functions/README.md)
# Identifiants dans la table profils (identifiant + mot_de_passe), entreprise ind_active = 1
